/**
 * Copyright (c) 2020-present NAVER Corp.
 * egjs projects are licensed under the MIT license
 */
import * as React from "react";
function copyText(text, copyInput) {
    if (navigator.clipboard) {
        navigator.clipboard.writeText(text).then(function () {
            alert("You have copied the code.");
        })["catch"](function () {
            copyTextarea(text, copyInput);
        });
    }
    else {
        copyTextarea(text, copyInput);
    }
}
function copyTextarea(text, copyInput) {
    var range = document.createRange();
    copyInput.value = text;
    range.selectNodeContents(copyInput);
    var selection = window.getSelection();
    selection.removeAllRanges();
    selection.addRange(range);
    copyInput.setSelectionRange(0, copyInput.value.length); // A big number, to cover anything that could be inside the element.
    copyInput.focus();
    document.execCommand('copy');
    alert("You have copied the code.");
}
export default function CopyButton(_a) {
    var tab = _a.tab, index = _a.index, onCopyText = _a.onCopyText;
    var textareaRef = React.useRef();
    var onClick = React.useCallback(function () {
        var text = onCopyText(tab, index);
        if (!text) {
            return;
        }
        copyText(text, textareaRef.current);
    }, [tab, index, onCopyText]);
    var onSelect = React.useCallback(function () {
        document.execCommand('copy');
    }, []);
    return (React.createElement("button", { className: "preview-copy", onClick: onClick },
        "Copy",
        React.createElement("textarea", { ref: textareaRef, onSelect: onSelect })));
}
;
//# sourceMappingURL=CopyButton.js.map